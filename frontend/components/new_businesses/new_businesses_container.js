import { connect } from 'react-redux';
import NewBusinesses from './new_businesses';

const msp = (state, ownProps) => (
    {
       
    }
)

const mdp = dispatch => (
    {

    }
)

export default connect(msp, mdp)(NewBusinesses)