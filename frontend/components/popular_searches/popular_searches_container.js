import { connect } from 'react-redux';
import PopularSearches from './popular_searches'

const msp = (state, ownProps) => (
    {

    }
)

const mdp = dispatch => (
    {

    }
)

export default connect(msp, mdp)(PopularSearches)